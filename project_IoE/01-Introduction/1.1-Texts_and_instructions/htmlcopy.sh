#!/bin/bash
wget google.html google.com
cat index.html | grep google > google.txt

#1. go to the url and download the page
#2. save the archive details in an table
#3. search the archive for words of interest
#4. build an relational database with the info gathered on 1, 2 & 3.
#5. build an meaning database for the info gathered on previous steps.
